name: CI Workflow

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: two
        run: make setup

      - name: three
        run: make test

      - name: four
        run: make lint
