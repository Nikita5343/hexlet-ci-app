jobs:
  build: # имя взято для примера
    runs-on: ubuntu-latest

    steps:
      # Клонируем репозиторий
      - name: setup
      run: make setup
      # Устанавливаем Node.js
      - name: lint
        run: make lint
      - name: test
        run: make test
      
