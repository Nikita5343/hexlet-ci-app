name: CI Workflow

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: two
        run: make setup

      - name: three
        run: make test

      - name: four
        run: make lint
