jobs:
  build: # имя взято для примера
    runs-on: ubuntu-latest

    steps:
      # Клонируем репозиторий
      - setup:
      run: make setup
      # Устанавливаем Node.js
      - lint:
        run: make lint
      - test:
        run: make test
